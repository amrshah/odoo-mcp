<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Assets are declared in __manifest__.py; this file keeps the load order explicit and hosts shared actions. -->
    <record id="action_hms_command_center" model="ir.actions.client">
        <field name="name">Command Centre</field>
        <field name="tag">hms_command_center</field>
    </record>
</odoo>
